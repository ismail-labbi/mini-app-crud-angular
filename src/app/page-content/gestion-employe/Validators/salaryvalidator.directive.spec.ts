import { SalaryvalidatorDirective } from './salaryvalidator.directive';

describe('SalaryvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SalaryvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
