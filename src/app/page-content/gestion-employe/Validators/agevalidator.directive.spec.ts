import { AgevalidatorDirective } from './agevalidator.directive';

describe('AgevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AgevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
